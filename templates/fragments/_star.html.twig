{% for i in 1..5 %}
    {% if i <= movie.rating %} <i class="bi bi-star-fill"></i>
        {% else %}
        <i class="bi bi-star"></i>
    {% endif %}
{% endfor %}
    <span class="ps-1">{{ movie.rating }}</span>